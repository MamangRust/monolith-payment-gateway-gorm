{
  "permissions": {
    "allow": [
      "Agent(Explore)",
      "Bash(cp *)",
      "Bash(wc *)",
      "Bash(tail *)",
      "Bash(which *)",
      "Bash(sqlc *)",
      "Bash(protoc *)",
      "Bash(make *)",
      "Bash(go *)",
      "Bash(do *)",
      "Bash(head *)",
      "Bash(done)",
      "Bash(/^package.*/a\\/\\/go:generate *)",
      "Bash(}' \"service/$svc/redis/interfaces.go\"\n      echo \"added go:generate to $svc/redis\"\n    fi\n  fi\n  \n  # repository interfaces\n  if [ -f \"service/$svc/repository/interfaces.go\" ]; then\n    if ! grep -q \"go:generate\" \"service/$svc/repository/interfaces.go\" 2>/dev/null; then\n      # use perl for multi-line insert after package line\n      perl -i -p -e 's/^)",
      "Bash($done=1' \"service/$svc/repository/interfaces.go\"\n      echo \"added go:generate to $svc/repository\"\n    fi\n  fi\n  \n  # service interfaces\n  if [ -f \"service/$svc/service/interfaces.go\" ]; then\n    if ! grep -q \"go:generate\" \"service/$svc/service/interfaces.go\" 2>/dev/null; then\n      perl -i -p -e 's/^)",
      "Bash($done=1' \"service/$svc/service/interfaces.go\"\n      echo \"added go:generate to $svc/service\"\n    fi\n  fi\ndone\n\n# also fix the card service repository (it uses mocks/card.go, so keep as-is)\n# and fix auth service missing mocks\nfor svc in auth; do\n  if [ -f \"service/$svc/repository/interfaces.go\" ]; then\n    if ! grep -q \"go:generate\" \"service/$svc/repository/interfaces.go\" 2>/dev/null; then\n      perl -i -p -e 's/^)",
      "Bash($1 *)",
      "Bash($done=1)",
      "Bash(then)",
      "Bash(fi)",
      "Bash(echo *)",
      "Bash(mkdir *)",
      "Bash(sed *)",
      "Bash(rm *)"
    ]
  },
  "$version": 4
}