apiVersion: v1
kind: Service
metadata:
  name: transaction
  namespace: payment-gateway
spec:
  selector:
    app: transaction
  ports:
    - name: http-metrics
      port: 8088
      targetPort: 8088
    - name: grpc
      port: 50057
      targetPort: 50057
  type: ClusterIP
