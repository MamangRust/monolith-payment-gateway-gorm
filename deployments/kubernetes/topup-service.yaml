apiVersion: v1
kind: Service
metadata:
  name: topup
  namespace: payment-gateway
spec:
  selector:
    app: topup
  ports:
    - name: http-metrics
      port: 8087
      targetPort: 8087
    - name: grpc
      port: 50058
      targetPort: 50058
  type: ClusterIP
